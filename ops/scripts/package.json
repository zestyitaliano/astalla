{
  "name": "ops-scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "dev-seed": "tsx src/dev-seed.ts",
    "run-weekly-snapshot": "tsx src/run-weekly-snapshot.ts",
    "build": "echo 'no build for ops scripts'",
    "lint": "echo 'no lint for ops scripts'",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests for ops scripts'"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "dayjs": "^1.11.13",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.15.7"
  }
}
